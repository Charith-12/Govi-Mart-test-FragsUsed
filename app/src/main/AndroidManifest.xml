<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.govimarttest">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ShowVegetablesActivity"></activity>
        <activity android:name=".WeatherReportActivity" />
        <activity
            android:name=".FeedActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity
            android:name=".HomeActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity android:name=".PasswordRecovary" />
        <activity android:name=".CongratulationActivity" />
        <activity android:name=".CreateAccountFinal" />
        <activity android:name=".NewAccountPassword" />
        <activity android:name=".NewAccountAditional3" />
        <activity android:name=".NewAccountConectivity" />
        <activity android:name=".NewAccountActivity" />
        <activity android:name=".LogInActivity" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">

            <!--
           <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>